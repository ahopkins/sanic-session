language: python
dist: xenial
sudo: false
matrix:
  include:
    - python: 3.7
      dist: xenial
      sudo: true
    - python: 3.6
      dist: xenial
    - python: 3.5
      dist: xenial
install:
    - "python setup.py install"
    - "pip install -r dev_requirements.txt"
script: pytest
